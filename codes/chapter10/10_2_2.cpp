#include <iostream>
#include <numeric>
#include <algorithm>
#include <vector>
#include <cstring>
using namespace std;

int main(int argc, char const *argv[])
{
    //1
    // vector<int> ivec;                     //空容器
    // fill_n(back_inserter(ivec), 10, 100); //设置ivec 10个100的值
    // for (const auto &v : ivec)
    // {
    //     cout << v << endl;
    // }

    //2
    //copy 必须保证 dst大于等于 src的大小

   //3
   // back_iteratot *iter=100 就相当于向后面添加一个元素
    return 0;
}
